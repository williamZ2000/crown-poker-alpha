# 项目规则（Codex 单一智能体版）v2

> v2 生效日期：2026-09-15（决策 #D41，采纳 project-init 新版方法论：六阶段门禁/迭代管理/反屎山六条/图纪律/术语表）
> v1 生效日期：2026-08-07（决策 #D10），v1 全文见 git 历史（commit 前 7d8ab6b）
> 本文件是项目的"宪法"，任何 AI 会话开始时必读并无条件遵守。

## 角色分工

| 角色 | 职责 | 说明 |
|------|------|------|
| 张总 | 唯一决策者、审批人、审核者 | 所有方案由张总拍板 |
| Codex | 项目管理 + 技术开发 + 质量把关（三位一体） | 承接文档/方案/进度与代码开发全部职责 |

Codex 是项目中唯一的 AI 工作单元，不引入平台间传话与双重审核环节。

---

## 1. 决策权归张总（优先级最高）

任何修改文件的操作前，必须执行五步闭环：

```
1. 出方案  → 说明：改什么文件、改什么内容、为什么改、影响范围
2. 列清单  → 给出本次改动的完整文件清单
3. 等审批  → 张总明确说"OK/同意/执行"之前，不动任何文件
4. 执行    → 严格按审批通过的方案执行，不做额外改动
5. 汇报    → 逐项对照方案确认完成，附变更摘要
```

**此规则优先级高于所有其他规则。** 已获审批的任务范围内，正常执行步骤无需再次逐项询问。

## 2. 启动必读

每次会话开始，按以下顺序读取：

```
1. PROJECT_RULES.md               ← 本文档（行为准则）
2. .workbuddy/session/CONTEXT.md  ← 当前上下文快照
3. .workbuddy/memory/MEMORY.md    ← 项目长期记忆 + 偏好
4. .workbuddy/tasks/tasks.md      ← 任务看板
5. README.md                      ← 项目总览与文档导航
```

读完后从 CONTEXT.md 的"下会话入口"接续，不要求张总复述背景。

## 3. 决策留痕与联动更新

每个重要决策必须完整留痕，通过以下文件形成闭环：

| 步骤 | 文件 | 操作 |
|------|------|------|
| 1 | `.workbuddy/memory/decisions.md` | 追加决策记录（日期/背景/决策/原因/影响） |
| 2 | 受影响的设计/开发文档 | 同步更新相关内容 |
| 3 | `docs/CHANGELOG.md` | 追加变更条目（关联决策编号） |
| 4 | `.workbuddy/session/CONTEXT.md` | 更新"最近决策"摘要 |
| 5 | `docs/trace/doc-code-map.md` | 确认映射关系仍正确 |
| 6 | `docs/diagrams/*.drawio` | 涉及流程/架构的变更，同步更新对应图 |

## 4. 修改原则：最小修改 + 扩展前重构

两条规则分工明确，缺一不可：

- **最小修改**：能用最小的更改解决问题就不做大改；不重构不需要重构的代码；不创建不需要的文件。管的是"不要过度设计"。
- **扩展前重构**：如果新需求必须**绕过**现有结构才能实现（workaround），方案必须先包含一个"改结构"步骤（独立提交），然后才加功能。管的是"不要欠结构债"。
- 判断标准只有一条：这次改动需不需要"绕过"现有结构？需要，就先改结构。

## 5. 产出规范

- 所有 .cs 文件必须包含中文注释，说明类/方法的用途
- 命名使用 PascalCase（C# 惯例）
- 可调参数优先配置化（ScriptableObject），不硬编码（原型期常量集中在 GameParams，M1 正式任务迁 ScriptableObject）
- 遵循 `docs/dev/architecture.md` 的四层架构与依赖规则（asmdef 编译期强制）
- 术语的唯一权威在 `docs/design/design.md` 术语表；任何改名 = 一次原子提交全仓替换 + grep 验证零残留

（待补充的规范在正式会话中按五步闭环追加到本节。）

## 6. 文档-产出依赖链（严格单向，不可跳级）

```
设计文档 (docs/design/design.md)
    → 开发文档 (docs/dev/architecture.md)
        → 代码实现 (Assets/Scripts/**/*.cs)
```

- 动手前，确认对应的开发文档已存在并已阅读
- 开发文档缺失或设计存在缺口 → 标记阻塞，通知张总，不猜测

## 7. 开发管线（六阶段门禁）

所有开发任务按以下管线执行。**三道关键门禁（S2 流程图 / S3 技术方案 / S5 审计）不过不放行。**

### 7.1 任务分级

| 级别 | 走法 |
|------|------|
| Feature（新功能） | 全管线 S1~S6 |
| Bugfix | 问题定位（issue-log 四段式）→ 修复方案过审 → 修复 + 回归测试 → S5 审计 → S6 |
| 重构 / 技术债 | 命中 TD 触发条件时启动，走 S3~S6 |
| 文档 / 内容 | S1 → S6，出图按需 |

### 7.2 六阶段

| 阶段 | 产物 | 出口门禁 | draw.io 图 |
|------|------|---------|-----------|
| S1 设计文档 | design.md 对应章节定稿 | 张总审批 | —（可选概念图） |
| S2 需求分析 | 边界清单、角色/场景、异常流分析 | **流程图过审** | 泳道流程图、状态图 |
| S3 技术文档 | architecture.md 模块设计 + **联动修改清单**（全量） | 技术方案过审 | 架构图、时序图、ER 图 |
| S4 代码开发 | 代码 + 单测 | 单测全绿 + 自审通过 | — |
| S5 代码审计 | 审计报告 | **审计问题清零，或登记为 ISSUE/技术债** | — |
| S6 结束 | DoD 清单执行 | 见 7.5 | — |

### 7.3 draw.io 图纪律

- 图源文件统一放 `docs/diagrams/*.drawio`，**入库版本控制**，与代码同仓库
- 通过 draw.io MCP 维护文件、打开编辑器供张总审图
- 每张图挂文档章节编号（如 architecture.md ↔ diagrams/architecture-overview.drawio）；文档变更触发图同步检查（联动第 6 处）
- 图是张总校验 AI 理解的最便宜手段——流程图看 30 秒即可发现理解偏差，纠错成本最低
- **图即合同**：架构/数据流图的每个模块框标注对应的实现坐标（文件名）；S5 审计硬项：图上每个框指得到代码、代码职责不超出所属框

### 7.4 S5 独立审计

- Feature 级强制（Bugfix 可选）：用**全新上下文的子代理**当审计员——未参与开发，拿技术文档与下方清单盲审代码
- 审计清单：与文档一致性 / 边界与错误路径 / 测试覆盖 / 依赖是否越界 / 新增技术债 / 死代码残留 / 图-代码双向索引有效性
- 审计发现要么修完清零，要么按技术债台账传统登记触发条件后放行

### 7.5 S6 DoD（结束的定义）

- [ ] 单测全绿；触及模块的测试覆盖不降
- [ ] 审计闭环（问题清零或已登记）
- [ ] 联动六处更新完毕（decisions / 相关文档 / CHANGELOG / CONTEXT / doc-code-map / diagrams）
- [ ] 联动修改清单逐项勾销
- [ ] commit + push（提交规范见 §10）
- [ ] CONTEXT.md 写收工快照

## 8. 迭代管理

每个里程碑（tasks.md 的 M-x）就是一次迭代：

| 阶段 | 内容 | 门禁 |
|------|------|------|
| I0 迭代立项 | 范围定义 + 全局影响分析 + **TD 台账触发条件核对** + 重构预算 → 产出迭代计划（任务分解/联动清单/还债计划） | 张总批范围 |
| 任务执行 | n 个任务逐个走六阶段管线 | 各任务门禁 |
| I1 迭代验收 | 整体回归测试 + 架构评审（依赖图比对实际依赖）+ 文档一致性总检 | 张总验收 |
| I2 迭代收尾 | 复盘 + TD 台账更新（本轮新增/偿还）+ 打 tag 基线 + 收工仪式 | — |

**规模红线：影响清单超过一屏，就拆迭代。** 屎山概率与迭代规模正相关。

## 9. 反屎山硬规则

1. **联动清单制度**：S3 的强制产物——从 doc-code-map 与依赖图推导**全量**联动修改清单（代码/文档/图/测试/配置），执行照单施工，S6 逐项勾销。清单缺失 = 门禁不放行。
2. **扩展前重构**：见 §4。命中 TD 触发条件的迭代，I0 时登记还债计划，先还再动工。
3. **死代码零容忍**：被替代的旧路径必须删除，不许注释掉"留着以后用"。档案室是 git 历史（有 tag 可回溯）和 docs/archive/，不是代码正文。
4. **术语表 + 原子重命名**：术语唯一权威在 design.md 术语表；任何改名 = 一次原子提交全仓替换 + grep 验证零残留。
5. **依赖图即合同**：架构图标明模块依赖方向；能自动化的检查自动化（模块编译隔离 / import lint 等），自动不了的进 S5 审计清单。
6. **回归安全网 + 原子提交**：每个 commit 必须编译通过 + 测试绿；重构与功能分开提交。没有这张网，规则 2/3 都会因"怕改坏"而流产。

## 10. Git 提交规范

格式：`[Codex] 类型(范围): 描述 (任务ID)`

示例：
```
[Codex] feat(card): implement Card model with suit/rank/score (M1-01)
[Codex] docs(arch): write architecture doc (M0-04)
```

- 任务 ID 尽量标注，便于追溯
- 一个提交只做一件事；文档与代码改动不混在一个提交里（除非用户明确要求）
- 重构提交与功能提交分开，各自可独立回退
- **收工仪式：commit 后必 push**（云端备份 GitHub williamZ2000/crown-poker-alpha）

## 11. 质量审核

- Codex 完成任务后自行审查（对照文档逐项检查），发现问题直接修复后汇报
- 涉及方向性的判断 → 出方案等张总审批，不自行决定
- 张总可随时抽查任意提交

## 12. 不确定就反问

- 不猜测张总的意图
- 不替张总做决定
- 遇到歧义、缺失信息、多种可行方案时，先问再动
